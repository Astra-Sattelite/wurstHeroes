package Itachi_P

import ClosureForGroups
import ClosureTimers
import RegisterEvents
import Fast

let itachId = 'H003'
let id = 'A019'
let sharinganPath = "sharingan3.mdx"

function spellCast(unit caster)
    

init 
    registerPlayerUnitEvent(EVENT_PLAYER_UNIT_DETECTED) ->
        let detectedUnit = GetDetectedUnit()
        if detectedUnit.getTypeId() == itachId
            print("Я пчелка 2")